import { Camera, Check, Mail, Save, ShieldCheck, UserRound } from "lucide-react";
import { PageHeader } from "@/components/dashboard/page-header";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { getAppUser } from "@/lib/auth";

export default async function ProfilePage(){const user=await getAppUser();return <main className="p-4 sm:p-6 lg:p-8"><PageHeader title="Profile" description="Manage your personal details and account preferences."/><div className="mt-7 grid gap-5 xl:grid-cols-[.7fr_1.3fr]"><Card className="p-6 text-center"><div className="relative mx-auto w-fit"><span className="grid size-24 place-items-center rounded-full bg-gradient-to-br from-violet-500 to-cyan-400 text-2xl font-bold">{user.name.split(' ').map(part=>part[0]).slice(0,2).join('')}</span><button className="absolute bottom-0 right-0 grid size-8 place-items-center rounded-full border-2 border-card bg-white text-slate-900"><Camera className="size-3.5"/></button></div><h2 className="mt-4 font-semibold">{user.name}</h2><p className="mt-1 text-xs text-muted-foreground">{user.email}</p><div className="mt-5 flex justify-center gap-2"><span className="rounded-full bg-violet-500/10 px-2.5 py-1 text-[10px] text-violet-300">Pro plan</span><span className="rounded-full bg-emerald-500/10 px-2.5 py-1 text-[10px] text-emerald-300">Verified</span></div></Card><Card className="p-6"><h2 className="text-sm font-semibold">Personal information</h2><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-[11px] text-muted-foreground">First name<Input className="mt-2" defaultValue={user.name.split(' ')[0]}/></label><label className="text-[11px] text-muted-foreground">Last name<Input className="mt-2" defaultValue={user.name.split(' ').slice(1).join(' ')}/></label><label className="text-[11px] text-muted-foreground sm:col-span-2">Email address<div className="relative mt-2"><Mail className="absolute left-3 top-3.5 size-3.5"/><Input className="pl-9" defaultValue={user.email}/><Check className="absolute right-3 top-3.5 size-3.5 text-emerald-400"/></div></label><label className="text-[11px] text-muted-foreground sm:col-span-2">Role / headline<Input className="mt-2" defaultValue="Software Engineering Student"/></label><label className="text-[11px] text-muted-foreground sm:col-span-2">Bio<textarea className="mt-2 min-h-24 w-full rounded-xl border border-input bg-white/[.035] p-3 text-sm" defaultValue="Building useful digital products and an ambitious graduation project."/></label></div><Button className="mt-5"><Save/> Save changes</Button></Card><Card className="p-6 xl:col-start-2"><div className="flex gap-3"><span className="grid size-10 place-items-center rounded-xl bg-emerald-500/10 text-emerald-300"><ShieldCheck className="size-4"/></span><div className="flex-1"><h2 className="text-sm font-semibold">Account security</h2><p className="mt-1 text-xs text-muted-foreground">Authentication is managed securely through Clerk.</p></div><Button variant="outline" size="sm">Manage security</Button></div></Card></div></main>}
